/**
 * Temporary switch for employee check-in/check-out face verification.
 * Keep client and server on the same value so the camera and API gate
 * cannot get out of sync.
 */
export const ATTENDANCE_FACE_CHECK_ENABLED = false;
