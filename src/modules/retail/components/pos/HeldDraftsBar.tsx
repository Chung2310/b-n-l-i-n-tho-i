import type { RetailOrder } from "../../types";
const money = (value: number) => new Intl.NumberFormat("vi-VN").format(value) + " ₫";
export default function HeldDraftsBar({ drafts, activeId, onOpen }: { drafts: RetailOrder[]; activeId?: string; onOpen: (draft: RetailOrder) => void }) { if (!drafts.length) return null; return <div><div className="mb-2 flex justify-between text-sm"><span className="font-semibold">Đơn treo</span><span className="text-slate-500">{drafts.length}/5</span></div><div className="flex gap-2 overflow-x-auto pb-1">{drafts.map((draft) => <button key={draft._id} onClick={() => onOpen(draft)} className={`shrink-0 rounded-xl border px-3 py-2 text-left text-sm ${activeId === draft._id ? "border-cyan-500 bg-cyan-50" : "bg-white"}`}><b>#{draft._id.slice(-6)}</b><span className="ml-2 text-slate-500">{money(draft.grandTotal)}</span></button>)}</div></div>; }
