import { AlertCircle, CheckCircle2, Copy } from "lucide-react";
export type ScanFeedbackKind = "success" | "not-found" | "duplicate" | "warning";
export function playScanTone(kind: ScanFeedbackKind) { try { const AudioContextClass = window.AudioContext || (window as any).webkitAudioContext; if (!AudioContextClass) return; const context = new AudioContextClass(), oscillator = context.createOscillator(), gain = context.createGain(); oscillator.frequency.value = kind === "success" ? 880 : kind === "duplicate" ? 520 : kind === "warning" ? 660 : 220; gain.gain.value = 0.04; oscillator.connect(gain); gain.connect(context.destination); oscillator.start(); oscillator.stop(context.currentTime + 0.08); } catch { /* silent fallback */ } }
export default function ScanFeedback({ kind, text }: { kind: ScanFeedbackKind; text: string }) { const Icon = kind === "success" ? CheckCircle2 : kind === "duplicate" ? Copy : AlertCircle; return <div role="status" className={`flex items-center gap-2 rounded-lg p-3 text-sm ${kind === "success" ? "bg-emerald-50 text-emerald-700" : kind === "duplicate" || kind === "warning" ? "bg-amber-50 text-amber-700" : "bg-red-50 text-red-700"}`}><Icon className="h-4 w-4" />{text}</div>; }
