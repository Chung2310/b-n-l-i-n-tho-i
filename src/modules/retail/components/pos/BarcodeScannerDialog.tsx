import React from "react";
import { Camera, X } from "lucide-react";
export default function BarcodeScannerDialog({ onScan, onClose }: { onScan: (value: string) => void; onClose: () => void }) {
  const videoRef = React.useRef<HTMLVideoElement>(null); const [error, setError] = React.useState("");
  React.useEffect(() => { let stream: MediaStream | null = null; let timer = 0; let stopped = false; const start = async () => { const Detector = (window as any).BarcodeDetector; if (!Detector) { setError("Trình duyệt chưa hỗ trợ quét mã bằng camera. Vui lòng nhập hoặc dùng máy quét."); return; } try { stream = await navigator.mediaDevices.getUserMedia({ video: { facingMode: "environment" }, audio: false }); if (!videoRef.current) return; videoRef.current.srcObject = stream; await videoRef.current.play(); const detector = new Detector({ formats: ["ean_13", "ean_8", "code_128", "qr_code"] }); const scan = async () => { if (stopped || !videoRef.current) return; try { const codes = await detector.detect(videoRef.current); const value = String(codes[0]?.rawValue || "").trim(); if (value) { stopped = true; onScan(value); onClose(); return; } } catch { /* camera frame not ready */ } timer = window.setTimeout(scan, 180); }; void scan(); } catch (cause) { setError(cause instanceof Error ? cause.message : "Không mở được camera."); } }; void start(); return () => { stopped = true; window.clearTimeout(timer); stream?.getTracks().forEach((track) => track.stop()); }; }, [onClose, onScan]);
  return <div className="fixed inset-0 z-[60] flex items-center justify-center bg-slate-950/70 p-4"><div role="dialog" aria-label="Quét mã sản phẩm" className="w-full max-w-lg rounded-3xl bg-white p-4"><div className="mb-3 flex items-center justify-between"><h2 className="flex items-center gap-2 font-bold"><Camera className="h-5 w-5" />Quét mã sản phẩm</h2><button onClick={onClose}><X /></button></div><video ref={videoRef} className="aspect-video w-full rounded-2xl bg-black object-cover" muted playsInline />{error && <p className="mt-3 rounded-xl bg-amber-50 p-3 text-sm text-amber-800">{error}</p>}<p className="mt-3 text-center text-sm text-slate-500">Đưa mã vào giữa khung hình.</p></div></div>;
}
