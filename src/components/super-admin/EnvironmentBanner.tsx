export function EnvironmentBanner({ environment }: { environment: "staging" | "production" }) { const production = environment === "production"; return <div role="banner" className={`px-4 py-2 text-center text-xs font-black uppercase tracking-[.25em] ${production ? "bg-red-600 text-white" : "bg-amber-400 text-amber-950"}`}>{production ? "Production — thao tác ảnh hưởng dữ liệu thật" : "Staging — môi trường kiểm thử"}</div>; }
