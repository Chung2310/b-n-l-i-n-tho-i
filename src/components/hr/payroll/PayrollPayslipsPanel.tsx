import { Download, Send } from "lucide-react";
export function PayrollPayslipsPanel({ canManage, publishedCount, onPublish, onExport }: { canManage: boolean; publishedCount: number; onPublish: () => void; onExport: (type: "detailed" | "insurance" | "pit" | "bank_transfer") => void }) {
  return <div className="flex flex-wrap items-center gap-2">{canManage && <button type="button" onClick={onPublish} className="inline-flex items-center gap-1.5 rounded-lg bg-emerald-600 px-3 py-2 text-sm text-white"><Send size={15} /> Publish ({publishedCount})</button>}{(["detailed", "insurance", "pit", "bank_transfer"] as const).map((type) => <button key={type} type="button" onClick={() => onExport(type)} className="inline-flex items-center gap-1.5 rounded-lg border bg-white px-3 py-2 text-sm"><Download size={15} /> {type}</button>)}</div>;
}