import { CompanyModel } from "../model/company.model";
export type TenantLifecycleStatus = "active" | "suspended" | "archived" | "scheduled-deletion";
export interface TenantRecord { code:string; name:string; ownerEmail:string; createdAt:Date; lifecycleStatus:TenantLifecycleStatus; lifecycleChangedAt?:Date; deletionScheduledAt?:Date|null; retentionEndsAt?:Date|null; deletionReason?:string; }
export interface TenantRepository { create(t:TenantRecord):Promise<TenantRecord>; list():Promise<TenantRecord[]>; get(c:string):Promise<TenantRecord|null>; update(c:string,u:Partial<TenantRecord>):Promise<TenantRecord|null>; }
const db: TenantRepository = { create: async t => (await CompanyModel.create(t)).toObject() as TenantRecord, list: async () => await CompanyModel.find({}).sort({createdAt:-1}).lean() as any, get: async c => await CompanyModel.findOne({code:c}).lean() as any, update: async(c,u) => await CompanyModel.findOneAndUpdate({code:c},{$set:u},{new:true,runValidators:true}).lean() as any };
const next:Record<TenantLifecycleStatus,TenantLifecycleStatus[]>={active:["suspended","archived"],suspended:["active","archived"],archived:["active"],"scheduled-deletion":[]};
const code=(v:string)=>{const c=String(v||"").trim().toUpperCase();if(!c)throw Error("Tenant code is required");return c}; const needed=(t:TenantRecord|null,c:string)=>{if(!t)throw Error(`Tenant ${c} not found`);return t};
export class TenantManagementService { constructor(private readonly tenants:TenantRepository=db) {} async create(v:Pick<TenantRecord,"code"|"name"|"ownerEmail">){const c=code(v.code),name=String(v.name||"").trim(),ownerEmail=String(v.ownerEmail||"").trim();if(!name||!ownerEmail)throw Error("Tenant name and owner email are required");if(await this.tenants.get(c))throw Error(`Tenant ${c} already exists`);const now=new Date();return this.tenants.create({code:c,name,ownerEmail,createdAt:now,lifecycleStatus:"active",lifecycleChangedAt:now,deletionScheduledAt:null,retentionEndsAt:null,deletionReason:""})} list(){return this.tenants.list()} async get(v:string){const c=code(v);return needed(await this.tenants.get(c),c)} async update(v:string,input:Pick<Partial<TenantRecord>,"name"|"ownerEmail">){const u:Partial<TenantRecord>={};if(input.name!==undefined){if(!String(input.name).trim())throw Error("Tenant name is required");u.name=String(input.name).trim()}if(input.ownerEmail!==undefined){if(!String(input.ownerEmail).trim())throw Error("Owner email is required");u.ownerEmail=String(input.ownerEmail).trim()}if(!Object.keys(u).length)throw Error("No tenant fields to update");const c=code(v);return needed(await this.tenants.update(c,u),c)} async transitionLifecycle(v:string,target:Exclude<TenantLifecycleStatus,"scheduled-deletion">){const c=code(v),t=needed(await this.tenants.get(c),c);if(!next[t.lifecycleStatus].includes(target))throw Error(`Invalid lifecycle transition from ${t.lifecycleStatus} to ${target}`);return needed(await this.tenants.update(c,{lifecycleStatus:target,lifecycleChangedAt:new Date()}),c)} async scheduleDeletion(v:string,reason:string,now=new Date(),impactPreview?:Record<string,unknown>,backupEvidenceId?:string){const c=code(v),t=needed(await this.tenants.get(c),c),r=String(reason||"").trim();if(t.lifecycleStatus!=="archived")throw Error("Tenant must be archived before deletion");if(!r)throw Error("Deletion reason is required");if(!impactPreview||!backupEvidenceId)throw Error("impact preview and backup evidence are required");const retentionEndsAt=new Date(now);retentionEndsAt.setUTCDate(retentionEndsAt.getUTCDate()+30);const deletionJob={status:"queued",impactPreview,backupEvidenceId};return needed(await this.tenants.update(c,{lifecycleStatus:"scheduled-deletion",lifecycleChangedAt:now,deletionScheduledAt:now,retentionEndsAt,deletionReason:r,deletionJob} as any),c)} async cancelDeletion(v:string){const c=code(v),t=needed(await this.tenants.get(c),c);if(t.lifecycleStatus!=="scheduled-deletion")throw Error("Tenant deletion is not scheduled");return needed(await this.tenants.update(c,{lifecycleStatus:"archived",lifecycleChangedAt:new Date(),deletionScheduledAt:null,retentionEndsAt:null,deletionReason:""}),c)} }