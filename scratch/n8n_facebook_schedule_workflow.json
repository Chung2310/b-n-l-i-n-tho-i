{
  "name": "iGen ERP - Facebook Scheduled Post",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "schedule-post",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "1cb383cf-8a71-46bd-89b9-dcd5c05c088f",
      "name": "Webhook - Receive from ERP",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [
        380,
        240
      ]
    },
    {
      "parameters": {
        "jsCode": "const items = $input.all();\n\nfor (const item of items) {\n  const body = item.json.body || item.json;\n\n  const cardId = body.cardId;\n  const title = body.title;\n  const bodyText = body.content || body.bodyText || \"\";\n\n  const mediaType = body.mediaType || \"none\";\n  const mediaUrl = body.mediaUrl || \"\";\n\n  let imageUrl = \"\";\n  let videoUrl = \"\";\n  if (mediaType === \"image\") {\n    imageUrl = mediaUrl;\n  } else if (mediaType === \"video\") {\n    videoUrl = mediaUrl;\n  }\n  if (!imageUrl && !videoUrl) {\n    imageUrl = body.imageUrl || \"\";\n    videoUrl = body.videoUrl || \"\";\n  }\n\n  // ── Parse scheduled_publish_time ──────────────────────────────────────────\n  const publishType = body.publishType || \"immediate\";\n  let isScheduled = publishType === \"scheduled\";\n  let scheduledPublishTime = null;\n\n  const parseScheduledTime = (raw) => {\n    if (!raw) return null;\n    const hasTimezone = /[Zz]|[+\\-]\\d{2}:?\\d{2}$/.test(String(raw));\n    const normalized = hasTimezone ? raw : `${raw}+07:00`;\n    const d = new Date(normalized);\n    return isNaN(d.getTime()) ? null : Math.floor(d.getTime() / 1000);\n  };\n\n  if (body.scheduledTime) {\n    scheduledPublishTime = parseScheduledTime(body.scheduledTime);\n    if (scheduledPublishTime) isScheduled = true;\n  } else if (body.scheduledDate) {\n    const timeStr = body.scheduledHour\n      ? `${body.scheduledDate}T${body.scheduledHour}`\n      : `${body.scheduledDate}T00:00:00`;\n    scheduledPublishTime = parseScheduledTime(timeStr);\n    if (scheduledPublishTime) isScheduled = true;\n  }\n\n  // ── Validate theo Facebook API constraints ────────────────────────────────\n  if (isScheduled && scheduledPublishTime) {\n    const nowSec = Math.floor(Date.now() / 1000);\n    const minAllowed = nowSec + 10 * 60;\n    const maxAllowed = nowSec + 30 * 24 * 3600;\n    if (scheduledPublishTime < minAllowed) {\n      scheduledPublishTime = minAllowed + 60;\n    } else if (scheduledPublishTime > maxAllowed) {\n      throw new Error(`Thời gian lên lịch không được vượt quá 30 ngày từ hiện tại.`);\n    }\n  }\n\n  // ── Auth & API URL ────────────────────────────────────────────────────────\n  const integration = body.integration || {};\n  const pageId = body.pageId || integration.pageId;\n  const accessToken = body.accessToken || integration.pageAccessToken;\n\n  if (!pageId || !accessToken) {\n    throw new Error(\"Missing Facebook Page ID or Access Token in integration data.\");\n  }\n\n  let facebookApiUrl = \"\";\n  let requestBody = {};\n  let postType = \"text\";\n\n  if (videoUrl && videoUrl.trim().startsWith(\"http\")) {\n    postType = \"video\";\n    facebookApiUrl = `https://graph.facebook.com/v19.0/${pageId}/videos`;\n    requestBody = { description: bodyText, file_url: videoUrl, access_token: accessToken };\n    if (isScheduled && scheduledPublishTime) {\n      requestBody.published = false;\n      requestBody.scheduled_publish_time = scheduledPublishTime;\n    } else {\n      requestBody.published = true;\n    }\n  } else if (imageUrl && imageUrl.trim().startsWith(\"http\")) {\n    postType = \"image\";\n    facebookApiUrl = `https://graph.facebook.com/v19.0/${pageId}/photos`;\n    requestBody = { caption: bodyText, url: imageUrl, access_token: accessToken };\n    if (isScheduled && scheduledPublishTime) {\n      requestBody.published = false;\n      requestBody.scheduled_publish_time = scheduledPublishTime;\n    } else {\n      requestBody.published = true;\n    }\n  } else {\n    postType = \"text\";\n    facebookApiUrl = `https://graph.facebook.com/v19.0/${pageId}/feed`;\n    requestBody = { message: bodyText, access_token: accessToken };\n    if (isScheduled && scheduledPublishTime) {\n      requestBody.published = false;\n      requestBody.scheduled_publish_time = scheduledPublishTime;\n    }\n  }\n\n  // Lưu pageId và cardId vào preparedData để Callback node dùng\n  item.json.preparedData = { cardId, pageId, postType, apiUrl: facebookApiUrl, body: requestBody };\n}\n\nreturn items;"
      },
      "id": "e6dd0508-bdf3-4eb7-a7eb-bfd17e75bc7b",
      "name": "JavaScript - Parse & Format Data",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        600,
        240
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $json.preparedData.apiUrl }}",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ $json.preparedData.body }}",
        "options": {
          "response": {
            "response": {
              "neverThrow": true,
              "responseFormat": "json"
            }
          }
        }
      },
      "id": "3be9a25b-de7b-402a-bf36-58c0677579f1",
      "name": "HTTP Request - Facebook Graph API",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.1,
      "position": [
        820,
        240
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={\n  \"status\": \"success\",\n  \"message\": \"Lên lịch đăng bài trên Facebook Page thành công qua n8n\",\n  \"data\": {\n    \"id\": \"{{ $json.id || $json.post_id }}\",\n    \"cardId\": \"{{ $node['JavaScript - Parse & Format Data'].json.preparedData.cardId }}\",\n    \"postType\": \"{{ $node['JavaScript - Parse & Format Data'].json.preparedData.postType }}\"\n  }\n}"
      },
      "id": "eaef0614-2c70-4f51-b0db-bcf5268c27e8",
      "name": "Respond to Webhook",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [
        1040,
        180
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $node['Webhook - Receive from ERP'].json.body?.callbackUrl || $node['Webhook - Receive from ERP'].json.callbackUrl || 'https://erp.igentechsolutions.com/api/v1/facebook/n8n-callback' }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "x-webhook-token",
              "value": "igen_n8n_secret_token_2026"
            },
            {
              "name": "Content-Type",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({\n  cardId: $node['JavaScript - Parse & Format Data'].json.preparedData.cardId,\n  postId: String($json.id || $json.post_id || ''),\n  postUrl: 'https://www.facebook.com/' + $node['JavaScript - Parse & Format Data'].json.preparedData.pageId + '/posts/' + String($json.id || $json.post_id || '')\n}) }}",
        "options": {}
      },
      "id": "callback-to-erp-node-id",
      "name": "Callback to ERP",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.1,
      "position": [
        1040,
        320
      ]
    }
  ],
  "connections": {
    "Webhook - Receive from ERP": {
      "main": [
        [
          {
            "node": "JavaScript - Parse & Format Data",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "JavaScript - Parse & Format Data": {
      "main": [
        [
          {
            "node": "HTTP Request - Facebook Graph API",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "HTTP Request - Facebook Graph API": {
      "main": [
        [
          {
            "node": "Respond to Webhook",
            "type": "main",
            "index": 0
          },
          {
            "node": "Callback to ERP",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": true,
  "settings": {
    "executionOrder": "v1"
  },
  "versionId": "1cb383cf-8a71-46bd-89b9-dcd5c05c088f",
  "id": "facebook-schedule-post-workflow"
}